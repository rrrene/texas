# <%= o.title %>

<% templates = [] %>

<% templates = templates_by_glob(o.glob) if o.glob %>
<% templates = o.templates if o.templates %>

<% templates.each_with_index do |template, index| %>
<%= render template %>

<%= scene_delimiter unless index == templates.size - 1 %>
<% end %>
